Run this command to create local sample workbooks:

```powershell
python scripts/create_fixtures.py
```

The generated `.xlsx` files are ignored by Git so real workbooks do not leak into the repository.
